---
import Link from "@components/Link.astro";

interface Props {
  title: string;
  href: string;
}

const { title, href } = Astro.props;
---

<Link
  {title}
  {href}
  class="inline-block tracking-wider supports-[hover:hover]:hover:scale-110"
/>
